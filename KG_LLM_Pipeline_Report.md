# 知识图谱大语言模型 (LLM) 全自动抽取管线技术报告

在本次重构中，我们将传统的「人工审核+正则规则」的死板抽取管线，全面升级为了基于大语言模型（LLM）的自动化高保真流水线，大幅降低了散点实体的数量，并突破了关系抽取的规则限制。

---

## 核心痛点与优化思路

| 维度 | 旧版规则管线的问题 | 升级后 LLM 管线的优势 |
|---|---|---|
| **术语审核** | 会保留成千上万的散点词汇（未人工审核则默认保留），导致大量毫无意义的普通词语混入节点，形成海量“孤岛节点”。 | LLM 精细审核 Top 300 高频候选，只保留航空航天专业核心节点，将 10,000+ 噪声节点精简至数百个核心术语节点。 |
| **关系抽取** | 必须满足严苛的“触发词”（如包含、属于）和距离限制，大量深层语义关系被过滤，关系数量极少（如 1700 个节点仅 26 条边）。 | 突破死板规则。只要同句中出现两个以上的核心节点，就由 LLM 结合上下文理解实体之间的潜在联系并提取，边密度大幅上升。 |
| **人工介入** | 需要暂停管线，人工打开 CSV 进行筛选、删减并重命名为 `terms_final.csv`。 | 完全零介入。用户在前端上传 PDF，后台自动一气呵成处理完所有步骤。 |

---

## 当前完整工作流 (Pipeline)

当用户在平台前端点击上传 PDF 并建立图谱时，后台自动触发以下 5 大核心步骤：

### Step 1: 文本解析与预处理 (`preprocess`)
* **怎么做的**：
  * 通过 PyMuPDF 提取 PDF 原始文字。
  * 遇到乱码页面，自动调用多模态视觉模型（OCR）进行后备文字识别。
  * 清洗乱码和回车符，并按照标点符号切分为单句（写入 `all_sentences.jsonl`）。

### Step 2: 统计学术语挖掘 (`extract_terms`)
* **怎么做的**：
  * 对全文进行分词处理（jieba）。
  * 结合 TF-IDF 和 PMI（互信息）算法，挖掘出文档中高频、高聚合度的词汇（如候选词“变构飞行器”）。
  * 这一步会生成大量生词候选，写入 `terms_clean.csv`。

### Step 3: LLM 核心术语审核 (`llm_review.py` / **NEW**)
* **怎么做的**：
  * 拦截 `terms_clean.csv` 中评分前 300 的候选词汇。
  * 将这批词汇打包交给 LLM，提示词赋予其“航空航天领域知识图谱专家”的人设。
  * **LLM 的任务**：
    1. 判断是否为专业名词。废话、介词等标记为 `keep="N"`（直接丢弃），从而实现散点降噪。
    2. 对标记保留的词重新分配标准实体类型（如 `Aircraft`, `Structure`, `Mechanism`）。
  * 审核结果保存为 `terms_final.csv`，作为本图谱唯一的专属高纯度字典。

### Step 4: 命名实体识别 (NER) (`ner.py`)
* **怎么做的**：
  * 遍历 Step 1 中切分好的每一句话。
  * 拿着 Step 3 刚刚生成的“纯净大模型字典”，去句子里进行精准匹配，圈出文中所有实体的出现位置。
  * 输出只包含核心实体的 `entities_raw.jsonl`，不再有几万条无关紧要的动词或无名节点。

### Step 5: LLM 上下文关系抽取 (`llm_relation.py` / **NEW**)
* **怎么做的**：
  * 按句子为单位进行扫描，抛弃原本的触发词正则搜索。
  * 筛选出**“同一句话中同时包含了 >= 2 个核心实体”**的句子。
  * 将这些高潜力句子以 20 句为一个 Batch 组装发送给 LLM。
  * **LLM 的任务**：
    结合整句话的语义，判断这两个实体间是否存在 `has_component`, `affects`, `uses_method` 等 8 种预定义关系中的一种，并格式化输出。
  * 最终将 LLM 找出的关系经过端点类型校验后，落盘为 `relations_clean.jsonl`，并由 `export.py` 导出为 ECharts 绘图需要的格式。

---

## 最终效果
通过这种 **“宽进严出的大模型实体清洗”** + **“基于上下文的大模型关系补全”**：
1. **彻底解决满屏幕散点的问题**：没有关系的孤岛节点大幅减少，呈现在画布上的都是有核心连接的紧密网络。
2. **全流程自动化**：上传 PDF -> 发起异步任务 -> 后端大模型流式处理 -> 页面自动轮询并在几分钟后展示完整图谱，完美契合 Web 平台的使用逻辑。
