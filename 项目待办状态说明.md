# 项目待办状态说明

更新时间：2026-04-22
项目：`KG_Lab`

## 一、整体说明

本文档用于记录当前知识图谱项目的待办完成情况，区分：
- 已完成
- 进行中
- 尚未开始

说明：以下状态结合了当前代码、数据产物和最近实现进度整理，不只是最初计划表。

---

## 二、已完成事项

### 1. 题目范围与基础本体
- 已确定项目围绕“变构飞行器”知识图谱展开。
- 已形成实体类型与关系抽取的整体方向。
- 代码中已存在实体识别、关系抽取、图谱导出、评估、应用骨架等模块目录。

### 2. 原始语料整理
- 已整理 PDF 原始数据集。
- 已完成文本抽取，生成 `data/text/` 下的中文文本。
- 已完成 ASCII 命名映射，生成 `data/text_ascii/`。
- 已生成文件名映射表：
  - `data/metadata/file_name_mapping_ascii.csv`

### 3. 文档元数据与句子级预处理
- 已生成文档元数据表：
  - `data/metadata/documents.csv`
- 已生成句子级语料：
  - `data/processed/sentences.jsonl`

### 4. 术语候选抽取
- 已实现术语抽取模块：
  - `terminology/term_extraction.py`
- 已完成：
  - n-gram 候选生成
  - 词频统计
  - TF-IDF 计算
  - PMI 计算
  - 基础规则过滤
- 已输出术语文件：
  - `data/terminology/terms_raw.csv`
  - `data/terminology/terms_clean.csv`

### 5. LLM 最终清洗能力接入
- 已新增大模型术语清洗模块：
  - `terminology/llm_clean.py`
- 已在 `config.json` 中新增 `term_cleaning` 配置。
- 已设计术语清洗专用提示词，约束模型只做：
  - 保留/删除
  - canonical 归一化
  - 实体类型修正
- 已生成相关输出文件：
  - `data/terminology/terms_clean_llm.csv`
  - `data/terminology/alias_llm.csv`

### 6. 实体抽取结果已落地
- 已存在实体抽取结果文件：
  - `data/entities/entities_raw.jsonl`
  - `data/entities/entities_clean.jsonl`
- 说明实体识别链路已经至少跑通过一版。

### 7. 关系抽取结果已落地
- 已存在关系抽取结果文件：
  - `data/relations/relations_raw.jsonl`
  - `data/relations/relations_clean.jsonl`
- 说明关系抽取链路已经至少跑通过一版。

### 8. 标注与评估相关基础文件已存在
- 已存在标注导入数据：
  - `data/annotations/doccano_import.jsonl`
  - `data/annotations/pred_entities.jsonl`
  - `data/annotations/pred_relations.jsonl`
- 已存在评估模块：
  - `evaluation/metrics.py`
- 已存在标注互操作模块：
  - `annotation/interop.py`

### 9. 知识图谱导出结果已存在
- 已存在图谱导出文件：
  - `data/kg/nodes.csv`
  - `data/kg/edges.csv`
  - `data/kg/kg.json`
  - `data/kg/kg.ttl`
- 已存在导出模块：
  - `kg/export.py`

### 10. 应用骨架已存在
- 已存在应用代码结构：
  - `app/main.py`
  - `app/templates/index.html`
  - `app/services/`
- 说明应用端基础骨架已经搭起来。

---

## 三、进行中事项

### 1. 术语库最终清洗
当前状态：进行中

已完成：
- 规则过滤已经做了多轮。
- LLM 清洗模块和提示词已经接入。
- 相关输出文件已经出现。

仍需确认：
- `terms_clean_llm.csv` 的实际质量是否稳定。
- `alias_llm.csv` 是否足够支撑后续实体标准化。
- 是否需要继续补少量中文噪声规则。

### 2. 候选术语人工筛选
当前状态：进行中

说明：
- 术语候选已经生成。
- 但还需要结合人工确认，形成最终高质量术语表。

### 3. 标注样本准备
当前状态：进行中

说明：
- 标注相关中间文件已经存在。
- 但规范化的 gold 标注集是否完整、是否达到计划数量，仍需确认。

---

## 四、尚未完全完成或仍需补强的事项

### 1. alias 表与实体类型映射表规范化
当前状态：未完全完成

虽然已有：
- `alias.csv`
- `alias_llm.csv`

但仍建议补齐：
- 最终版 alias 表
- 最终版实体类型映射表
- 明确哪些别名合并到哪个 canonical

### 2. 实体识别规则完善
当前状态：未完全完成

虽已有 `candidate_extraction/ner.py` 和实体结果文件，但仍需确认：
- 参数表达是否覆盖充分
- 结构术语是否覆盖充分
- 控制方法类术语是否覆盖充分
- longest match 和冲突消解是否达到最终要求

### 3. 关系抽取规则完善
当前状态：未完全完成

虽已有 `candidate_extraction/relation.py` 和关系结果文件，但仍需继续检查：
- 触发词规则是否足够稳定
- 关系方向是否正确
- 关系去重与归一化是否充分
- 是否达到目标关系规模

### 4. 结果质量评估
当前状态：未完全完成

仍需补强：
- 实体 Precision / Recall / F1
- 关系 Precision / Recall / F1
- 明确评估样本规模
- 误差分析报告

### 5. 人工标注规范
当前状态：未完全完成

仍需整理：
- 实体边界规则
- 关系方向规则
- 冲突处理规则
- gold 标注文件说明

### 6. Neo4j 落地验证
当前状态：未完全完成

虽然已有图谱导出结果，但仍需确认：
- 是否已成功导入 Neo4j
- 导入脚本是否稳定
- 查询和展示是否可用

### 7. 应用层功能完整性
当前状态：未完全完成

虽有应用骨架，但仍需确认以下是否真正可用：
- 首页统计
- 实体搜索
- 实体详情页
- 子图可视化
- 关系浏览
- 证据句追溯
- 来源文献展示

### 8. 最终提交材料
当前状态：未完成

仍需准备：
- 成员分工说明
- 独创性声明
- 技术报告
- 演示视频脚本与录制
- 最终提交打包

---

## 五、建议的下一步优先级

建议按以下顺序继续推进：

### 第一优先级
1. 确认 `terms_clean_llm.csv` 与 `alias_llm.csv` 质量
2. 形成最终术语表与 alias 表
3. 锁定实体类型映射规则

### 第二优先级
4. 回头检查实体抽取结果质量
5. 回头检查关系抽取结果质量
6. 补做评估指标与误差分析

### 第三优先级
7. 验证 Neo4j 导入
8. 完善应用展示页面
9. 整理文档、报告、视频和提交材料

---

## 六、简版结论

### 已经做好的
- 语料抽取与整理
- 元数据与句子级预处理
- 术语候选抽取
- LLM 术语最终清洗能力接入
- 实体/关系中间结果落地
- 图谱导出结果落地
- 应用骨架存在

### 还没彻底做完的
- 最终术语表定稿
- alias 与类型映射定稿
- 实体与关系质量评估
- Neo4j 最终落地验证
- 应用功能完善
- 技术报告与最终提交材料
