# 知识图谱大作业开发指南（变构飞行器方向，2人组版）

> 本指南按照课程大作业要求、你当前项目现状和 2 人组交付目标进行重构。
> 目标不是只“抽几个三元组”，而是做出一个**完整、可评估、可展示、可运行的领域知识图谱系统**，并最终落到一个**知识图谱应用程序**上。

---

## 1. 先明确作业要求到底在要什么

根据作业截图，本次大作业的核心要求有 4 条：

### 1.1 必做成果

1. 从给定文档集中任选文档，构建一个**完整的知识图谱**。
2. **最多 2 人一组**，你当前是 2 人组。
3. 人工构建：
   - **概念不少于 500 个**
   - **关系不少于 1000 条**
4. 自动抽取：
   - 必须提供**算法源代码**
   - 可以基于 LLM 辅助，但**不能只用 LLM**
5. 评估数据：
   - 至少标注 **200 个概念**
   - 至少标注 **400 个关系**
   - 用于算法评估
6. 2 人组额外要求：
   - **必须构建一个知识图谱应用程序**
7. 提交内容：
   - 小组成员及分工说明
   - 独创性声明
   - 大作业文档
   - 源代码
   - 4–5 分钟讲解视频

### 1.2 评分点如何倒推开发重点

作业评分结构：

- 文档 40 分
- 源代码 40 分
- 视频 20 分

因此你们不能只做“算法”，也不能只做“图谱可视化”。正确策略是：

- **文档上讲清楚：为什么这样设计、如何评估、结果如何**
- **代码上拿出完整 pipeline + 应用程序**
- **视频上展示一个清晰可用的知识图谱系统闭环**

---

## 2. 你当前项目到了哪一步

结合现有仓库，当前状态如下。

### 2.1 已完成部分

你已经有了一个不错的工程骨架：

- `preprocess/extract.py`
  - PDF 文本抽取
  - 纯文本层优先
  - 乱码检测
  - 视觉模型 OCR 兜底
- `preprocess/clean.py`
  - 文本清洗
  - 句子切分
  - 基于 `jieba` 的分词
- `scripts/run_pipeline.py`
  - 预处理阶段可运行
- `data/text/`
  - 已经积累了多篇飞行器、变构飞行器相关文本

### 2.2 未完成但必须补齐的部分

以下模块目前基本还是占位：

- `terminology/term_extraction.py`：术语抽取未实现
- `candidate_extraction/ner.py`：实体识别未实现
- 关系抽取模块：尚未真正落地
- `annotation/interop.py`：标注导入导出未实现
- `kg/export.py`：图谱导出与图数据库接入未实现
- `evaluation/metrics.py`：评估未实现
- 应用层：**完全缺失**

### 2.3 当前最大风险

你现在最大的风险不是“抽取效果不够强”，而是：

1. **作业闭环不完整**：还没有从文本 → 实体/关系 → 图谱 → 应用 → 评估的完整闭环。
2. **2 人组要求未满足**：还没有知识图谱应用程序。
3. **500 概念 / 1000 关系如果全靠人工从零硬写，会非常累**。
4. **200 概念 / 400 关系评测集**还没有明确标注方案。

所以接下来必须从“模块开发思维”切换到“作业交付思维”。

---

## 3. 正确的项目目标：做一个可交付的完整系统

你们的最终目标建议定义为：

> 面向变构飞行器领域文献，构建一个包含概念层、本体层、实例层和关系层的领域知识图谱，并实现一个支持检索、查询、关系浏览和问答辅助的知识图谱应用程序。

这个目标比“做抽取算法”更完整，也更适合答辩。

---

## 4. 建议你们选定的题目方向

建议把题目聚焦为：

## 4.1 推荐题目

**变构飞行器领域知识图谱构建与应用系统设计**

或者稍微学术一点：

**基于领域文本抽取与规则融合的变构飞行器知识图谱构建及应用实现**

### 4.2 为什么选“变构飞行器”

因为你当前已有语料明显集中在：

- 变构飞行器
- 飞行器气动设计
- 飞行器结构设计
- 飞行控制
- 飞行动力学

这个主题有几个优点：

1. 语料集中，便于术语抽取
2. 实体类别容易定义
3. 关系类型清晰
4. 很适合做应用演示，例如：
   - 某类变形方式涉及哪些结构
   - 某种控制策略适用于哪些场景
   - 某种气动布局带来哪些性能特征

---

## 5. 你们最后要交付的系统长什么样

你们最终应该做成一个“五层闭环”：

### 5.1 数据层

输入：PDF / 提取后的 txt 文本

### 5.2 抽取层

- 文本清洗
- 分句分词
- 术语抽取
- 实体识别
- 关系抽取

### 5.3 图谱层

- 实体标准化
- 别名归并
- 三元组构建
- 导出 TTL / CSV / JSON
- 导入 Neo4j

### 5.4 评估层

- 用人工标注集评估实体识别
- 用人工标注集评估关系抽取
- 输出 Precision / Recall / F1

### 5.5 应用层

必须有一个可演示的应用程序，建议做成下面这种轻量方案：

#### 推荐方案：知识图谱检索与浏览 Web 应用

功能至少包含：

1. **实体检索**
   - 搜索“变构飞行器”
   - 返回该实体的简介、别名、相关关系
2. **关系浏览**
   - 展示该实体连出去和连进来的关系
3. **图谱子图可视化**
   - 展示该实体周边 1~2 跳子图
4. **问题查询模板**
   - 如“某控制技术适用于哪些变体飞行器？”
   - 如“某种变形方式与哪些气动布局相关？”
5. **文章来源追溯**
   - 每个实体或关系附带来源文档或证据句

这类应用最容易做、最符合课程要求、最适合 4–5 分钟视频展示。

---

## 6. 不要走偏：你们该采用什么技术路线

这次作业不建议搞特别重的深度学习训练闭环。因为时间有限，最稳妥的路线是：

## 6.1 推荐路线：规则 + 统计 + 少量模型 + 人工校验

### 实体抽取

采用三段式：

1. **术语候选抽取**
   - TF-IDF
   - PMI
   - C-value 或 n-gram 频次
2. **规则/词典实体识别**
   - 基于领域词表高召回识别
3. **必要时加轻量模型**
   - `bert-base-chinese` 或 `hfl/chinese-roberta-wwm-ext` 微调
   - 但不把希望全部压在模型上

### 关系抽取

建议分两类：

1. **规则型关系抽取**
   - 如“X 是 Y 的一种”
   - “X 采用 Y”
   - “X 包括 Y”
   - “X 影响 Y”
   - “X 用于 Y”
2. **句子级分类或模板抽取**
   - 先用实体共现筛句
   - 再按触发词进行关系判断

### 最重要的原则

> 这次作业可以不是最先进，但一定要完整、可解释、可复现、可展示。

---

## 7. 本体应该怎么设计

你现在最需要的是先把本体收敛，不能一开始无限扩张。

建议采用“核心 8 类实体 + 10 类关系”的方案。

## 7.1 推荐实体类型

1. `Aircraft`
   - 定义：表示飞行器、航天器、变体飞行器、空天飞行器等飞行平台或飞行器类别。
   - 判定原则：凡是描述某种飞行平台、构型平台、飞行器总类或具体飞行器概念，优先标为 `Aircraft`。
   - 例子：变体飞行器、跨域变构飞行器、空天飞行器、无人机、航天器。
2. `Structure`
   - 定义：表示飞行器的物理结构、部件、翼面、外形布局与构型组成。
   - 判定原则：凡是明确属于飞行器部件、结构单元、几何外形组成部分的术语，标为 `Structure`。
   - 例子：机翼、翼型、翼面、机身、尾翼、折叠构型、展开构型、后掠角。
3. `Mechanism`
   - 定义：表示变形机制、驱动机理、变构方式、切换逻辑、结构变换机理等。
   - 判定原则：强调“如何变”“靠什么变”“变形机理是什么”的术语，标为 `Mechanism`。
   - 例子：机构驱动、折展变构、翼型变形、变后掠、伸缩翼、柔性翼。
4. `ControlMethod`
   - 定义：表示控制策略、控制算法、决策方法、优化方法和学习方法。
   - 判定原则：凡是具体控制方法、优化求解方法、智能决策方法，一律标为 `ControlMethod`。
   - 例子：模型预测控制、自适应控制、滑模控制、动态规划、遗传算法、深度强化学习、DDPG。
5. `Performance`
   - 定义：表示飞行性能、控制品质、气动特性、稳定性与效率类指标。
   - 判定原则：凡是表示“飞得怎么样”“控制得怎么样”“性能好不好”的名词，标为 `Performance`。
   - 例子：升阻比、稳定性、机动性能、鲁棒性、气动性能、控制品质、响应速度。
6. `Mission`
   - 定义：表示飞行任务、任务场景、飞行阶段和任务目标。
   - 判定原则：凡是描述飞行器执行什么任务、处于什么任务阶段的术语，标为 `Mission`。
   - 例子：起降、巡航、机动、滑翔、跨域飞行、再入、着陆、航天运输任务。
7. `Parameter`
   - 定义：表示飞行状态量、控制参数、气动参数、环境参数等可量化变量。
   - 判定原则：凡是可作为模型输入、控制变量、飞行状态量或性能计算参数的术语，标为 `Parameter`。
   - 例子：攻角、速度、高度、马赫数、侧滑角、气动参数、转动惯量。
8. `Document`
   - 定义：表示论文、文献、项目名称、表格来源或知识来源对象。
   - 判定原则：当句子中出现明确文献、项目或研究来源名称，并且后续要做来源追溯时，标为 `Document`。
   - 例子：SAW项目、SMS项目、文献[33]、航天运输三次浪潮图、某综述论文。

## 7.2 推荐关系类型

1. `is_a`
   - 含义：A 是 B 的一种或属于 B。
   - 适用场景：定义句、分类句、概念归属句。
2. `part_of`
   - 含义：A 是 B 的组成部分。
   - 适用场景：部件到整体的从属关系。
3. `has_component`
   - 含义：A 包含 B、由 B 组成、具有 B 这一部件或构型组成。
   - 适用场景：整体到部件的结构组成关系。
4. `uses_method`
   - 含义：A 采用 B 方法或策略实现某目标。
   - 适用场景：飞行器、任务、决策对象与控制方法之间的采用关系。
5. `has_mechanism`
   - 含义：A 具有 B 机制、变形方式或驱动方式。
   - 适用场景：飞行器与变构机理、驱动机理之间的关系。
6. `affects`
   - 含义：A 影响 B。
   - 适用场景：参数、结构、机制、方法对性能或状态造成影响。
7. `applies_to`
   - 含义：A 适用于 B。
   - 适用场景：控制方法、决策方法、设计方案适用于某类飞行器或任务。
8. `has_parameter`
   - 含义：A 具有参数 B，或 A 与参数 B 强关联。
   - 适用场景：飞行器、模型、方法与参数之间的关联。
9. `supports_mission`
   - 含义：A 支持 B 任务。
   - 适用场景：构型、方法、结构、性能对任务目标的支撑关系。
10. `documented_in`
   - 含义：A 记载于 B 文献或项目来源中。
   - 适用场景：实体、关系或方法与文献来源之间的追溯关系。

### 7.3 当前项目最终本体结论

- 最终题目：`变构飞行器领域知识图谱构建与应用系统设计`
- 研究范围：以变构飞行器、跨域变构飞行器、空天飞行器相关文本为核心语料，覆盖结构、机理、控制、性能、任务、参数与文献来源。
- 实体类型：固定为 `Aircraft`、`Structure`、`Mechanism`、`ControlMethod`、`Performance`、`Mission`、`Parameter`、`Document` 共 8 类。
- 关系类型：固定为 `is_a`、`part_of`、`has_component`、`uses_method`、`has_mechanism`、`affects`、`applies_to`、`has_parameter`、`supports_mission`、`documented_in` 共 10 类。
- 后续所有术语筛选、人工标注、自动抽取、图谱构建、应用展示均以这套定义为准，不再随意扩张本体范围。

### 7.4 为什么这样设计

因为这个集合：

- 足够支撑 500 概念、1000 关系
- 足够适合规则抽取
- 足够适合应用展示
- 不会把本体做得过于复杂

---

## 8. 如何满足“500 概念、1000 关系”

这里要明确一点：

> 作业要求里的“概念”和“关系”，不一定全部来自模型自动抽取，也不要求全是严格意义上的本体类概念。

对于课程作业，通常可接受如下理解：

- 500 个实体/术语/概念节点
- 1000 条关系边

因此正确做法是：

### 8.1 概念节点来源

概念节点由三部分组成：

1. **人工设计的类别体系**
   - 如实体类型、一级类、二级类
2. **从语料中抽取出的领域术语**
   - 例如“翼型变换”“可变后掠翼”“模型预测控制”等
3. **人工筛选和归一化后的有效实体**

### 8.2 关系来源

关系边也由三部分构成：

1. 规则抽取
2. 人工补充
3. 从文献章节标题、定义句、枚举句中批量生成

### 8.3 一个现实可行的数量策略

建议目标设定为：

- 候选术语 1200+
- 清洗后有效实体 600~800
- 自动抽取关系 1500+
- 清洗后保留 1000~1300

这样你最后提交时会更稳。

---

## 9. 人工标注集应该怎么做

这是你文档里必须讲清楚的重点。

## 9.1 标注目标

至少完成：

- 200 个概念标注
- 400 个关系标注

## 9.2 推荐标注方案

### 实体标注

从 20~30 篇文本里抽样句子，标注以下类型：

- 飞行器
- 结构
- 机制
- 控制方法
- 性能
- 任务
- 参数
- 文献

### 关系标注

优先标注高频且好判断的关系：

- `is_a`
- `part_of`
- `has_component`
- `uses_method`
- `affects`
- `applies_to`
- `supports_mission`

### 9.3 2 人组如何分工标注

- A 同学先标第 1 轮
- B 同学复核并修订
- 对冲突样本形成“标注规范补充表”

这样文档里就能写出：

- 标注流程
- 一致性控制
- 人工复核机制

这会明显提升文档分数。

---

## 10. 2 人组最关键：应用程序到底做什么

这是你现在最该补上的部分。

## 10.1 推荐应用形态

建议直接做一个：

**变构飞行器知识图谱查询与辅助分析系统**

### 10.2 最低可交付功能

1. **首页统计面板**
   - 实体数
   - 关系数
   - 文献数
   - 实体类型分布
2. **实体搜索**
   - 输入关键词搜索实体
3. **实体详情页**
   - 实体名称
   - 实体类型
   - 别名
   - 相关文献
   - 相邻关系
4. **图谱可视化页**
   - 展示某一实体周边子图
5. **关系查询页**
   - 按关系类型筛选
6. **证据句展示**
   - 显示该关系来自哪句话、哪篇文献

### 10.3 加分功能

如果有余力，再加一个自然语言查询框：

- “模型预测控制适用于哪些变体飞行器？”
- “可变后掠翼会影响哪些性能指标？”

底层不用真做 LLM 问答，只要把自然语言映射成图查询模板即可。

### 10.4 技术建议

- 后端：`FastAPI`
- 前端：
  - 简单方案：`FastAPI + Jinja2 + ECharts / PyVis`
  - 或 `Streamlit`
- 图数据库：`Neo4j`
- 数据交换：`JSON`

如果你想要最省时间，我建议：

> **FastAPI + Neo4j + PyVis/ECharts**

因为这套结构最像一个正式“应用系统”。

---

## 11. 2 人应该怎么分工最合理

建议不要按“一个人做前端、一个人做后端”来分，因为这样容易两边脱节。

建议按“图谱构建主线”和“应用与评估主线”分工。

## 11.1 分工建议

### A 同学：抽取与图谱构建负责人

负责：

- 文本预处理
- 术语抽取
- 实体识别
- 关系抽取
- 三元组构建
- 数据清洗与标准化

### B 同学：标注、评估与应用负责人

负责：

- 标注规范整理
- 标注集构建与复核
- Precision / Recall / F1 评估
- Neo4j 数据接入
- Web 应用开发
- 演示视频与结果展示

### 11.2 共同完成

- 本体设计
- 核心关系定义
- 文档撰写
- 答辩视频录制

这样写到文档里会显得分工明确、协作合理。

---

## 12. 从现在开始你们应该按什么顺序做

下面给你一个真正可执行的开发顺序。

## 第一阶段：锁定作业边界

### 目标

把题目、语料范围、本体和应用目标定死。

### 产出

- 题目名称
- 实体类型表
- 关系类型表
- 应用功能清单
- 2 人分工表

### 你现在就应该完成的事情

1. 选定最终题目
2. 固定 8 类实体、10 类关系
3. 决定应用程序最小功能集

---

## 第二阶段：把数据资产整理好

### 目标

形成统一语料库。

### 要做的事情

1. 检查 `data/text/` 下文本质量
2. 去重
3. 统一命名
4. 给每篇文献分配文档 ID
5. 建立文献元数据表

### 产出

- `data/text/` 标准文本集
- 文献清单 CSV

---

## 第三阶段：先做术语表，再做抽取

### 目标

先拿到一个能支撑整个项目的领域词表。

### 要做的事情

1. 用 TF-IDF / n-gram / PMI 提取候选术语
2. 人工筛选术语
3. 按实体类型归类
4. 构建 alias 表

### 产出

- 领域术语表
- 别名表
- 实体候选库

这一步非常重要，因为后面：

- 规则 NER 要用它
- 关系抽取要用它
- 实体归一化要用它
- 应用检索也要用它

---

## 第四阶段：实现可交付的实体与关系抽取

### 实体抽取

建议先上规则和词典：

- 精确率不会太差
- 容易解释
- 很适合作业答辩

### 关系抽取

优先做模式规则：

- “X 是 Y 的一种” → `is_a`
- “X 采用 Y” → `uses_method`
- “X 包括 Y” → `has_component`
- “X 影响 Y” → `affects`
- “X 用于 Y” → `applies_to`

### 产出

- 实体抽取结果 JSON
- 关系抽取结果 JSON
- 候选三元组 CSV / JSON

---

## 第五阶段：人工标注与评估

### 目标

补齐课程要求中的评估部分。

### 要做的事情

1. 构建 200 概念标注集
2. 构建 400 关系标注集
3. 输出预测结果
4. 计算 Precision / Recall / F1
5. 分析误差来源

### 文档里要写的内容

- 标注方式
- 标注范围
- 数据量
- 评估指标
- 实验结果
- 误差分析

---

## 第六阶段：构建图谱并接入应用

### 目标

把抽取结果变成可查询的知识图谱。

### 要做的事情

1. 实体归一化
2. 实体去重
3. 关系标准化
4. 生成节点表和边表
5. 导入 Neo4j
6. 编写查询接口

### 产出

- 图数据库
- 基本查询接口
- 图谱统计结果

---

## 第七阶段：开发知识图谱应用程序

### 目标

满足 2 人组要求，并形成视频展示重点。

### 页面建议

1. 项目首页
2. 实体查询页
3. 图谱浏览页
4. 关系分析页
5. 文献证据页

### 演示时最好展示的场景

1. 搜索“变构飞行器”
2. 查看它的相关结构、控制方法、性能指标
3. 展示周边子图
4. 展示一条关系对应的来源句
5. 展示几个典型图查询问题

---

## 13. 你们的视频应该怎么录

视频只有 4–5 分钟，所以一定要短而完整。

## 13.1 推荐脚本

### 第 1 分钟：问题背景与目标

- 为什么做变构飞行器知识图谱
- 数据来源是什么
- 最终目标是什么

### 第 2 分钟：系统流程

- 文本预处理
- 术语抽取
- 实体关系抽取
- 图谱构建
- 应用展示

### 第 3 分钟：评估结果

- 标注规模
- Precision / Recall / F1
- 误差分析一句话总结

### 第 4–5 分钟：应用演示

- 搜索
- 子图展示
- 关系追溯
- 查询问答模板

这比单纯讲代码更容易得分。

---

## 14. 文档应该怎么写才容易拿高分

你的最终技术报告建议按以下结构写：

1. 课题背景与研究意义
2. 任务定义与作业要求对应关系
3. 数据来源与预处理
4. 本体设计
5. 术语抽取方法
6. 实体识别方法
7. 关系抽取方法
8. 知识图谱构建与存储
9. 知识图谱应用设计与实现
10. 标注集与评估方法
11. 实验结果与误差分析
12. 分工说明
13. 总结与展望

这份结构和评分点是高度对齐的。

---

## 15. 你现在最该做的不是继续发散，而是立刻收敛

如果你现在继续无限扩展技术栈，很容易把时间耗尽。

你现在最合理的策略是：

### 必保项

1. 文本预处理跑通
2. 术语抽取落地
3. 规则实体抽取落地
4. 规则关系抽取落地
5. 图谱导入 Neo4j
6. 做出可运行应用程序
7. 做完评估
8. 写好文档

### 可选增强项

1. 加轻量 Transformer NER
2. 加更漂亮的前端
3. 加自然语言问答入口

优先级一定不能反过来。

---

## 16. 接下来建议的直接开发任务

下面是你现在最适合立刻执行的任务清单。

### 任务 1：完善数据与本体

- 整理文本文件清单
- 定义实体类型
- 定义关系类型
- 输出术语分类模板

### 任务 2：落地术语抽取

- 在 `terminology/term_extraction.py` 中实现：
  - TF-IDF
  - n-gram 候选统计
  - 基于频次和长度筛选

### 任务 3：落地规则实体识别

- 在 `candidate_extraction/ner.py` 中实现：
  - 基于术语表的字典匹配
  - 输出实体 span、类型、来源句

### 任务 4：新增关系抽取模块

- 基于触发词和句法模板抽取关系
- 输出三元组 + 证据句

### 任务 5：构建图谱与应用

- 导出节点表、边表
- 导入 Neo4j
- 开发 Web 页面

### 任务 6：补齐评估

- 导入人工标注
- 计算 P/R/F1
- 生成结果表格

---

## 17. 一句话结论

你这个项目现在最正确的方向，不是继续写零散模块说明，而是把整个工程收敛成：

> **“变构飞行器领域知识图谱构建 + 图数据库落地 + Web 应用展示 + 标注评估闭环”的完整课程项目。**

只要你围绕这个目标推进，你的文档、代码、视频三部分就会统一起来，而且会非常贴合作业要求。

---

## 18. 下面我建议你接着做什么

如果继续由我来帮你推进，我建议下一步直接做下面 3 件事：

1. **重构 `README.md`**：改成项目总览、运行方式、目录结构、演示目标
2. **重构 `techs.md`**：改成技术选型说明、理由、分工、最小可交付方案
3. **重构 `流程图.puml`**：把“知识图谱应用程序”和“2 人协作流程”补进去

如果你愿意，我下一步可以继续直接把这 3 个文件也改好。