# 知识图谱大作业开发指南（飞行器方向）

> 说明：本指南基于课堂课件技术栈说明与大作业要求整理，面向实现一个领域知识图谱（Domain-specific KG），可直接作为项目 README / 开发规范使用。

## 1 目标与交付物

- 目标：从提供的文献/手册中抽取并构建一个完整的领域知识图谱（飞行器 / 变构飞行器 / 维修），要求：
  - 人工构建概念（实体）不少于 500 个；关系不少于 1000 条。
  - 提供人工标注样本：至少 200 个概念、400 个关系，用于评估。
  - 提供自动抽取算法源代码（允许辅助 LLM 但不可仅用 LLM）、技术报告、可复现运行脚本与 4–5 分钟演示视频。

## 2 时间规划（建议）
- 范围与本体设计：1–3 天
- 数据收集与预处理：2–4 天
- 自动候选抽取（NER/RE/远程监督）：3–7 天
- 标注与质检（并行）：7 天
- KG 构建、存储与可视化：2–4 天
- 评估与技术报告：2–4 天

## 3 推荐技术栈（一览）

- Python 环境：Python 3.8+
- 文本抽取与 OCR：`PyMuPDF` / `pdfminer.six`，图片 OCR 使用 `Tesseract` + `pytesseract`
- 预处理：`regex`、`nltk`、`jieba`（中文分词）、sentence-split utils
- 术语抽取：`scikit-learn`（TF-IDF）、`gensim`、`RAKE`/`YAKE`、自实现的 C-value/PMI
- NER：规则词典 + `spaCy`；微调模型用 HuggingFace `transformers`（BERT/SpanBERT/roberta）+ `datasets`；序列解码可用 `torchcrf`
- 关系抽取（RE）：依存句法规则（`spaCy`）+ 基于句子分类的深度模型（`transformers`）；可用远程监督扩充训练集（Snorkel 思路）
- 共指消解与实体链接：`neuralcoref`（可选）或基于上下文相似度的自实现方法；构建 alias/词表用于消歧
- 存储与可视化：小规模 RDF 用 `rdflib`（TTL/JSON-LD）；生产级查询/可视化用 `Neo4j` + `py2neo`/`neo4j` 驱动；可视化还可用 `networkx`、`pyvis`
- 标注工具：`doccano` / `label-studio`
- 评估：`sklearn.metrics`（precision/recall/f1）
- 开发/管理：`venv`/`conda`、`requirements.txt`、Git、`Makefile` 或 `run_pipeline.py`

## 4 推荐工作流（详尽步骤）

1. 定义范围与本体（Ontology）
   - 明确子域（例如：变构飞行器 / 飞行器 / 维修）。
   - 列出核心实体类型与关系集，并为每类写 1–2 行判定准则（用于标注说明）。
   - 示例实体类型见第 7 节。

2. 数据准备（PDF → 文本）
   - 使用 `PyMuPDF`/`pdfminer.six` 提取文本，必要时进行 OCR。
   - 文本清洗：统一换行、去除页眉页脚、合并断行、中文分词/英文 tokenization、句子切分。

3. 术语表与字典构建
   - 用 TF-IDF、RAKE/YAKE、C-value/PMI 提取候选术语；人工合并、去重并产生 alias 列表。
   - 术语表用于：NER 字典、实体消歧、远程监督种子三元组匹配。

4. 自动候选抽取
   - NER：先用规则匹配与词典提高召回，再用微调 Transformer 提升质量；标注格式采用 BIO。
   - RE：结合语法规则（基于依存）与句子级分类器（CNN/RNN/Transformer）；对远程监督生成的弱标注使用多实例学习或注意力机制降噪。

5. 标注与质检
   - 使用 `doccano`/`label-studio` 进行实体与关系标注。
   - 标注规范要点：BIO、优先最长匹配、标注方向（头实体 → 关系 → 尾实体）、互检规则（每 100 条中抽 10% 两人复核）。

6. KG 构建与融合
   - 去重与规范化：实体归一化（词表/ID）、属性值格式化。
   - 输出：TTL 或 JSON-LD（供 RDFlib 使用），并将核心子图导入 Neo4j 以便查询与可视化。

7. 评估与质量控制
   - 指标：三元组与关系抽取分别计算 Precision / Recall / F1。
   - 质检：抽样人工复核、冲突消解规则、属性值一致性检查。

## 5 标注规范（要点）

- 实体标注：BIO 格式；尽量标注最长实体单元（Longest match），但在注释中允许拆分子实体。
- 关系标注：指明方向并标注关系触发句或支持证据句（便于后续训练）。
- 同义词/缩略词：在词表中记录 alias，标注时使用标准化实体 ID。
- 标注质量：每 100 条标注至少双人复核 10%，统计一致率并纠正偏差。

## 6 项目目录模板（建议）

- dataset/
  - raw/（原始 PDF）
  - text/（提取后的纯文本）
  - term_list/（领域术语 CSV）
  - annotations/（entities.jsonl、relations.jsonl）
  - kg/（ttl / jsonld）
- src/
  - preprocess.py
  - term_extract.py
  - ner.py
  - re.py
  - build_kg.py
  - run_pipeline.py
  - evaluate.py
- requirements.txt
- README.md
- docs/report.pdf
- video/

## 7 推荐本体（核心实体与关系示例）

- 实体类型（示例）：Aircraft、Component、Subsystem、Fault、MaintenanceAction、Tool、Parameter、Document、Person/Role
- 关系示例：has_component、part_of、causes、symptom_of、requires_tool、performed_by、has_specification、documented_in、related_to

（将这些作为初始骨架，后续可根据语料扩展子类与属性）

## 8 简要实现建议与工具代码位置

- PDF→文本：`src/preprocess.py`：负责提取、清洗、句子切分。
- 术语抽取：`src/term_extract.py`：TF-IDF / RAKE / C-value 实现与人工合并接口。
- NER：`src/ner.py`：包含规则匹配模块与 Transformer 微调入口（使用 HuggingFace）。
- RE：`src/re.py`：规则抽取 + 训练/推理接口。
- KG 构建：`src/build_kg.py`：实体合并、导出 TTL/JSON-LD、Neo4j 导入脚本。
- 评估：`src/evaluate.py`：计算 P/R/F1，并输出详细分类报告。

## 9 快速运行（示例）

1. 建议使用虚拟环境并安装依赖：

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

2. 抽取单篇样例并运行小样例 pipeline：

```bash
python src/preprocess.py --input dataset/raw/sample.pdf --output dataset/text/sample.txt
python src/term_extract.py --input dataset/text/sample.txt --output dataset/term_list/sample_terms.csv
python src/ner.py --mode predict --input dataset/text/sample.txt --output dataset/annotations/sample_entities.jsonl
python src/re.py --mode predict --input dataset/annotations/sample_entities.jsonl --output dataset/annotations/sample_relations.jsonl
python src/build_kg.py --entities dataset/annotations/sample_entities.jsonl --relations dataset/annotations/sample_relations.jsonl --out dataset/kg/sample.ttl
```

（示例命令为骨架，实际脚本参数请在各脚本中实现或按需调整）。

## 10 评估与提交要求对齐

- 教师评分点：文档（40 分）、源代码（40 分）、视频（20 分）。
- 文档（技术报告）应包含：任务定义、本体设计、数据来源、方法论、评估、样例结果、复现步骤。
- 源代码：应能复现核心 pipeline，遵守代码规范并提供快速启动脚本（如 `run_pipeline.py --sample`）。

## 11 质量保障与常见注意事项

- 远程监督噪声：使用多实例学习 / 注意力权重聚合来降低错误标注影响。
- 实体合并：在合并前做上下文相似度判断并保留 alias 列表。
- 标注一致性：提供标注示例与反例（edge cases），并组织互检样例集。

## 12 附录：标注示例（简短）

- 文本："发动机叶片出现裂纹导致振动增大，需更换叶片并复检传动轴。"
  - 实体：`发动机叶片` (Component)、`裂纹` (Fault)、`振动` (Fault)、`传动轴` (Component)
  - 关系：`裂纹` `symptom_of` `发动机叶片`；`requires_tool` `更换叶片` `Tool:手动工具`

---

如果你需要，我可以接下来：
- 选项 A：生成一个最小可运行的项目骨架（`src/` + `requirements.txt` + `run_pipeline.py`）。
- 选项 B：基于“变构飞行器”子域，输出更加详细的本体与 10 个标注样例（供标注参考）。

请选择要继续的选项，或告诉我你希望我直接把 `requirements.txt` 和 `run_pipeline.py` 一并生成。
