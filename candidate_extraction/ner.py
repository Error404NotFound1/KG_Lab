"""NER 训练与推理示例（基于 transformers 的微调占位）"""

def train_ner(train_data: str, model_out: str = "models/ner"):
    print(f"训练 NER：{train_data} -> {model_out} （未实现）")


def predict_ner(text: str, model_path: str = "models/ner"):
    print(f"用模型 {model_path} 对文本进行 NER（未实现）")
    return []
