这份大作业指南非常专业且全面，涵盖了从数据预处理、本体设计到知识抽取、存储与评估的完整知识图谱构建生命周期。结合你提供的课件资料，你的作业主要属于**领域知识图谱（Domain-specific Knowledge Graph, DKG）**的构建范畴，它要求知识获取的质量高、粒度细，并且通常需要领域专家的参与（在本项目中即为你制定的本体和标注规范）。

以下我将根据你的大作业工作流（步骤），为你无比详尽地提取并映射课件中需要用到的核心知识点：

### 1. 目标定义与本体设计（对应指南步骤 1-2）

**课件知识点映射：模式设计（Schema Design）与知识表示**
在开始抽取前，你需要先定义飞行器/维修领域的“骨架”。
*   **本体与实例的关系**：课件指出，本体（Ontology）刻画了人们认知一个领域的基本框架，而知识图谱富含实体及关系实例。框架与实例好比骨骼与血肉的关系，在建设初期，模式（Schema）定义实质上就是在完成本体定义的任务。
*   **节点定义（概念与实体）**：
    *   **概念（Concept）**：指代特定类别，具有共同特征的实体分组（如你定义的 `Aircraft`, `Fault`, `Tool` 等推荐实体类型）。
    *   **实体（Entity）**：概念的实例，是独立存在的基础（如“波音747”、“发动机叶片裂纹”）。
*   **边定义（关系与属性）**：
    *   **关系（Relation）**：侧重实体之间的联系（如你列出的 `has_component`, `causes` 等）。
    *   **约束规则定义**：你需要为关系定义适用范围（例如 domain 和 range），如“包含部件”的主体应为飞行器或子系统，客体应为部件。

### 2. 术语表构建（对应指南步骤 7.2）

**课件知识点映射：领域短语与词汇挖掘**
你的指南建议使用 TF-IDF 等方法提取术语建立词表。课件中对此有非常详细的自动化/半自动化特征打分方法：
*   **TF-IDF（词频-逆文档频率）**：用于挖掘能够有效代表某篇文档特征的短语。一个词的重要程度与其在文档中的词频正相关，与在外部文档中的出现频率反相关。
*   **C-value 与 NC-value**：课件推荐使用 C-value 来优化词频统计，它可以解决父短语（如“支持向量机”）和子短语（如“向量”）的重复统计偏差；NC-value 则进一步结合了上下文单词的权重来发现高质量短语。
*   **PMI（点互信息）**：刻画短语组成部分之间经常一起搭配的一致性。如果两部分联合出现的概率远大于独立随机共现的概率，说明这是一个有意义的专业短语。
*   **同义词与缩略词挖掘**：术语表还需要包含 alias（别名）。课件提到可以通过基于模式匹配（如“X 简称 Y”）或在线百科（如百科 Infobox）的方法获取同义词和缩略词。

### 3. 命名实体识别（NER）（对应指南步骤 7.3）

**课件知识点映射：实体指代抽取与序列标注**
指南要求使用规则词典或 Transformer 提取实体。在课件中，这是**文本结构化**的关键一步。
*   **BIO 标注法**：这是监督学习中最常用的序列标记任务格式。B 表示实体的起始位置（Begin），I 表示中间或结束位置（Inside），O 表示非实体（Outside）。你在使用 doccano 或 label-studio 进行人工标注时，底层数据通常会转换为这类格式。
*   **基于深度学习的 NER 框架**：课件中详解了主流架构，包含分布式表示层（词向量/字向量）、上下文编码器（如 RNN/BiLSTM，用于考虑前后字符相互影响）、以及标签解码器（通常是 **CRF 条件随机场**）。
*   **CRF（条件随机场）的作用**：CRF 是一种充分考虑输出标签之间关系的序列标注模型，能有效建模预测标签之间的约束关系（例如避免在 O 之后直接出现 I-PER），极大提高预测准确率。

### 4. 关系抽取（RE）与自动候选扩展（对应指南步骤 7.4）

**课件知识点映射：关系抽取与远程监督（Distant Supervision）**
这是你作业中最核心的算法实现部分，指南中特别提到了基于句子分类和 Distant Supervision。
*   **基于模式（Pattern）的抽取**：可以分为专家定义模式和自动学习模式。例如使用语法模式“NP1 导致 NP2” ⟹ `<NP1, causes, NP2>`。其关键在于降低人工代价并保证质量。
*   **远程监督（Distant Supervision）**：课件中对此有重点讲解。其基本假设是：**给定一个三元组 `<s, r, o>`，则任何包含实体对 `(s, o)` 的句子都在某种程度上描述了该实体对之间的关系**。你可以利用刚建立的术语表或少量种子数据，自动从海量飞行器文献中回标句子，产生大规模弱标注样本，从而扩增训练数据。
*   **深度学习关系分类**：输入包含实体对的句子，通过 CNN 或 RNN 提取上下文特征，再通过 Softmax 产生预定义关系类别上的概率分布。课件还提到了**注意力机制（Attention）**，用于为不同句子赋予权重，从而有效降低远程监督带来的噪声数据。

### 5. 共指消解与实体消歧（后处理）（对应指南步骤 7.6）

**课件知识点映射：实体链接（Entity Linking）与知识融合**
*   **实体链接目标**：将文本中的一个命名实体指代（Mention，比如指南中的“发动机”）与图谱中的标准实体（Entity）对应起来，消除歧义。
*   **局部与全局模型**：不仅要考虑指代词与候选实体的上下文相似度（局部），还可以考虑文本中多个指代词所链接实体间的联系（全局模型）。

### 6. 质检、评估与存储（对应指南步骤 5, 6, 7）

**课件知识点映射：质量控制与三元组存储**
*   **算法评估指标**：对于关系抽取，课件明确给出了计算公式：
    *   **精确率（Precision）**：$TP / (TP + FP)$
    *   **召回率（Recall）**：$TP / (TP + FN)$
    *   **F1 值**：精确率与召回率的调和平均 $2*P*R / (P+R)$。单一考察 P 或 R 都是不全面的，F1 是综合指标。
*   **质量维度**：你的人工互检机制对应课件中的**一致性检测**和**准确性抽样评估**。多源数据合并时必须进行“冲突消解”和“属性值规范化”。
*   **存储格式**：图谱在底层通常采用 **RDF 三元组模型**表示，即 `<主体(Subject)，谓词(Predicate)，客体(Object)>` 的形式（例如 `<发动机, has_component, 叶片>`）。对于大规模领域图谱，课件推荐使用图数据库（Graph DB，如你提到的 Neo4j）来进行多步遍历和关联查询。

**总结建议**：
在你的大作业答辩或技术报告撰写中（占40分），强烈建议在描述系统架构时，采用课件中的规范术语（如**自底向上的数据驱动结合自顶向下的知识引导**、**远程监督机制**、**BiLSTM-CRF 序列标注** 等）。这不仅能体现你对课程理论的深入理解，还能完美贴合你指南中的“创新性与规范性”评分点，助你拿到高分。