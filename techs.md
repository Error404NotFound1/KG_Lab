# 技术选型说明（面向 2 人组知识图谱大作业）

> 本文档说明本项目的技术路线、模块选型理由、最小可交付方案，以及 2 人组协作分工建议。

---

## 1. 技术路线总览

本项目不追求最复杂的模型，而追求：

- 满足课程作业要求
- 能够完整跑通
- 结果可解释
- 可以评估
- 可以做应用展示

因此本项目采用如下总体路线：

> **规则 + 统计 + 轻量模型补充 + 人工校验 + 图数据库应用落地**

这条路线相比纯大模型方案更符合课程要求，也比重训练复杂深度模型更稳妥。

---

## 2. 各模块技术选型

## 2.1 文本获取与预处理

### 已选技术

- `PyMuPDF`
- `pdfminer.six`
- OpenAI 兼容视觉模型 OCR（兜底）
- `re`
- `jieba`

### 选型理由

#### `PyMuPDF`

优点：

- 读取 PDF 文本层速度快
- 能获取页面级文本
- 适合批量处理文档

#### `pdfminer.six`

优点：

- 对某些编码异常的 PDF 兼容性更好
- 可作为第二层容错方案

#### 视觉模型 OCR

优点：

- 当 PDF 文本层失效或乱码时可兜底
- 能提高文档可用率

注意：

- OCR 只是文本获取手段，不是知识抽取主算法
- 不影响“不能只用 LLM”的要求

#### `jieba`

优点：

- 中文任务中轻量、稳定、易用
- 适合术语抽取前的分词支持

---

## 2.2 术语抽取

### 推荐技术

- `scikit-learn` 的 TF-IDF
- n-gram 频次统计
- PMI
- 可选 C-value

### 选型理由

术语抽取的目标不是做开放领域关键词提取，而是为了：

1. 生成领域术语候选集
2. 构建实体词典
3. 辅助规则实体识别
4. 辅助关系抽取和实体归一化

#### TF-IDF

适合快速筛出领域高频、具有区分度的术语。

#### n-gram 频次统计

适合中文短语候选发现，尤其适合“变构飞行器”“可变后掠翼”“模型预测控制”这类术语。

#### PMI

适合判断短语内部搭配紧密度，帮助筛掉伪组合词。

#### C-value

如果时间允许可以增加，用于提升嵌套术语识别质量；如果时间不足，可不作为首批必做项。

### 最终建议

首版先实现：

- n-gram 候选生成
- TF-IDF 排序
- PMI 过滤
- 人工筛选

这样已经足够支撑课程项目。

---

## 2.3 实体识别（NER）

### 推荐技术

- 词典匹配
- 正则规则
- 可选 Transformer 微调

### 选型理由

本项目更适合先做**规则型实体识别**，再决定是否补充模型。

#### 词典匹配

优点：

- 与术语表天然联动
- 容易解释
- 容易调试
- 对课程作业非常友好

#### 正则规则

适合识别：

- 参数表达式
- 缩略词
- 部分控制方法名称
- 特征性结构表达

#### Transformer 微调

可选模型：

- `bert-base-chinese`
- `hfl/chinese-roberta-wwm-ext`

作用：

- 作为增强项，提高泛化能力
- 不作为项目成败的唯一依赖

### 最终建议

首版先落地：

- 基于术语表的最长匹配实体识别
- 实体类型映射
- span 输出
- 证据句保留

如果后期还有时间，再增加模型版 NER。

---

## 2.4 关系抽取（RE）

### 推荐技术

- 触发词规则
- 句式模板匹配
- 实体共现过滤
- 可选分类模型

### 选型理由

知识图谱作业中的关系抽取，最关键的是：

- 关系类型清楚
- 抽取结果可解释
- 能产出足够多且较准确的关系

#### 触发词规则

例如：

- “是……的一种” → `is_a`
- “包括/包含” → `has_component`
- “采用” → `uses_method`
- “影响” → `affects`
- “适用于” → `applies_to`

优点：

- 容易实现
- 容易解释
- 便于调试和误差分析

#### 句式模板匹配

适合科技文本中高频定义句、枚举句、结构句。

#### 实体共现过滤

先根据句中实体对筛候选关系，可以明显减少噪声。

#### 分类模型

如果时间允许，可以加入一个句子级关系分类器做增强；但不是必须。

### 最终建议

首版先完成：

- 基于实体共现的候选生成
- 基于触发词和模板的关系抽取
- 输出三元组和证据句

---

## 2.5 标注与评估

### 推荐技术

- `doccano` 或 `label-studio`
- 自定义 JSON/JSONL 导入导出
- `scikit-learn` 或自定义评估脚本

### 选型理由

课程要求明确要提供：

- 至少 200 个概念标注
- 至少 400 个关系标注
- 算法评估结果

因此标注和评估是必做项。

#### `doccano` / `label-studio`

优点：

- 上手快
- 适合实体、关系标注
- 可导出结构化格式

#### 自定义转换脚本

必要性：

- 标注平台导出格式往往不能直接接入当前代码
- 需要将标注数据转为项目内部统一格式

#### 评估脚本

需要至少支持：

- Precision
- Recall
- F1

并建议输出：

- 按实体类型的评估结果
- 按关系类型的评估结果
- 错误案例分析

---

## 2.6 图谱构建与存储

### 推荐技术

- 中间格式：`JSON` / `CSV`
- 图谱导出：`rdflib`
- 图数据库：`Neo4j`

### 选型理由

#### `JSON` / `CSV`

优点：

- 便于调试
- 便于人工检查
- 易于导入图数据库

#### `rdflib`

优点：

- 适合导出 RDF / TTL
- 能体现知识图谱标准表达方式
- 文档里好写

#### `Neo4j`

优点：

- 适合做图谱查询与可视化
- 有成熟的 Cypher 查询语言
- 非常适合课程展示
- 容易做 Web 应用接入

### 最终建议

内部统一格式建议先用：

- `entities.json`
- `relations.json`
- `nodes.csv`
- `edges.csv`

最后再导出：

- `kg.ttl`
- 导入 Neo4j

---

## 2.7 应用程序实现

### 推荐技术

- 后端：`FastAPI`
- 图查询：`neo4j` Python 驱动
- 可视化：`PyVis` / `ECharts`
- 可选前端：Jinja2 模板或轻量前端页面

### 选型理由

2 人组必须做知识图谱应用程序，因此应用层必须尽量：

- 快速实现
- 易于演示
- 结构清楚
- 便于接入图数据库

#### `FastAPI`

优点：

- 开发快
- API 清晰
- 易于和 Python 数据处理代码衔接

#### `Neo4j` 驱动

优点：

- 可直接执行图查询
- 易于构造实体详情页和关系查询页

#### `PyVis` / `ECharts`

优点：

- 可快速展示子图关系
- 很适合答辩演示

### 推荐应用功能

最小功能集：

1. 实体搜索
2. 实体详情
3. 子图可视化
4. 关系筛选查询
5. 证据句展示
6. 图谱统计面板

---

## 3. 为什么不建议把纯 LLM 当主方案

虽然项目中 OCR 已经使用了视觉模型，但抽取主算法不应完全依赖 LLM，原因包括：

1. 课程要求明确指出不能只用 LLM
2. LLM 输出稳定性不足，批量抽取结果不易复现
3. 关系数量和证据句对齐较难严格控制
4. 评估时难以解释误差来源

因此更适合的方式是：

- 用规则、统计和可解释方法作为主干
- 必要时让 LLM 参与辅助清洗、辅助校对或 OCR

---

## 4. 最小可交付技术方案

为了确保按时完成，推荐采用下面这套最小方案。

## 4.1 必做模块

1. PDF 文本抽取与清洗
2. 术语抽取
3. 规则实体识别
4. 规则关系抽取
5. 图谱导入 Neo4j
6. 标注与评估
7. Web 应用程序

## 4.2 可选增强模块

1. Transformer NER
2. 关系分类模型
3. 自然语言查询接口
4. 更复杂的前端界面

### 原则

先保证闭环，再追求高级方法。

---

## 5. 2 人组分工建议

建议按“主线任务”分工，而不是单纯按“前端/后端”分工。

## 5.1 成员 A：图谱构建主线

负责：

- 文本预处理
- 术语抽取
- 实体识别
- 关系抽取
- 三元组构建
- 数据清洗与归一化

## 5.2 成员 B：评估与应用主线

负责：

- 标注规范整理
- 标注集构建
- 评估实现
- Neo4j 接入
- Web 应用开发
- 可视化页面

## 5.3 双方共同负责

- 本体设计
- 核心关系定义
- 结果分析
- 技术报告撰写
- 视频录制

---

## 6. 当前项目代码与技术栈映射

### 已经落地的部分

- `preprocess/extract.py`
  - `PyMuPDF`
  - `pdfminer.six`
  - OpenAI 兼容视觉模型 OCR
- `preprocess/clean.py`
  - `re`
  - `jieba`
- `scripts/run_pipeline.py`
  - 流程入口

### 需要继续补齐的部分

- `terminology/term_extraction.py`
- `candidate_extraction/ner.py`
- 关系抽取模块
- `annotation/interop.py`
- `kg/export.py`
- `evaluation/metrics.py`
- 应用层代码

---

## 7. 推荐实现优先级

### 第一优先级

- 术语抽取
- 规则实体识别
- 规则关系抽取
- 图谱数据生成

### 第二优先级

- 标注导入导出
- 评估脚本
- Neo4j 导入
- 应用程序开发

### 第三优先级

- 模型增强
- 界面优化
- 查询增强

---

## 8. 一句话技术结论

本项目最合理的技术路线是：

> **以预处理、术语词表、规则抽取和图数据库应用为主线，以标注评估作为质量保证，以轻量模型作为可选增强，最终完成一个面向变构飞行器领域的知识图谱构建与应用系统。**