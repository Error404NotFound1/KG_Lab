# 变构飞行器领域知识图谱构建与应用系统设计 — 技术报告

---

## 第一章 课题背景与研究意义

### 1.1 研究背景

变构飞行器（Morphing Aircraft）是当代航空航天工程中最具前瞻性的技术方向之一。与传统固定构型飞行器不同，变构飞行器能够在飞行过程中主动改变自身的气动外形——包括机翼后掠角、展弦比、翼面弯度、翼型剖面等关键几何参数——从而在不同飞行阶段（起飞、巡航、机动、着陆）获得最优的气动性能。这一特性赋予了飞行器跨速域、跨任务域的自适应飞行能力，被视为下一代航空器发展的核心技术路径。

变构飞行器领域的研究高度交叉，涉及空气动力学、结构力学、材料科学、飞行控制、多学科优化设计等多个学科。相关的学术知识分散在大量非结构化的技术文献中，包括学术专著、期刊论文、技术报告等。研究人员在进行文献调研时，往往需要花费大量时间进行人工检索和信息整合，效率低下且容易遗漏关键知识关联。

### 1.2 研究意义

知识图谱（Knowledge Graph）是一种以图结构组织领域知识的技术手段，能够将碎片化的实体（概念、术语）和它们之间的语义关系显式地建模为节点-边-节点的三元组形式。在变构飞行器领域构建知识图谱，具有以下重要意义：

1. **知识组织与检索**：将分散在数十篇文献中的专业术语、结构部件、控制方法、性能指标等信息统一组织为可查询的图结构，支持精确的语义检索。
2. **关系发现与推理**：通过图谱的关系边，可以发现隐含的知识关联，例如"某种控制方法适用于哪些飞行器构型""某种变形机制会影响哪些性能指标"。
3. **辅助科研决策**：研究人员可以通过图谱快速了解某一技术方向的上下文关联，辅助研究方案设计。
4. **学术交流与教学**：可视化的知识图谱为该领域的学术交流和课程教学提供了直观的知识展示工具。

### 1.3 项目目标

本项目的核心目标是：面向变构飞行器领域的学术文献，设计并实现一套完整的知识图谱构建与应用系统。具体包括：

- 从非结构化的 PDF 文献中自动提取文本并进行预处理
- 基于统计方法（TF-IDF、PMI）和大语言模型（LLM）混合策略进行领域术语抽取
- 实现基于词典匹配和正则规则的实体识别（NER）
- 实现基于触发词和多重约束的关系抽取（RE）
- 构建标准化的知识图谱并支持多格式导出（JSON、CSV、TTL）及 Neo4j 图数据库导入
- 开发一个交互式的 Web 应用系统，支持图谱可视化、实体检索、子图探索和证据追溯
- 建立人工标注的金标准数据集，对算法进行定量评估（Precision、Recall、F1）

---

## 第二章 系统总体架构

### 2.1 架构概述

本系统采用"七阶段流水线"架构，从原始 PDF 文献出发，经过层层处理，最终落地为可交互的 Web 应用和可量化评估的学术成果：

```
阶段 1: preprocess  — PDF 文本提取与清洗
阶段 2: terms       — 候选术语统计抽取（TF-IDF + n-gram + PMI）
阶段 3: finalize    — LLM 智能精筛 + 规则二次定稿
阶段 4: extract     — 全量实体识别（词典匹配 + 正则规则）
阶段 5: clean       — 实体强规则过滤与类型回写
阶段 6: relation    — 四重约束触发词关系抽取
阶段 7: kg          — 图谱导出（JSON/CSV/TTL/Neo4j）
```

此外还有两个辅助阶段：
- `annotate`：导出 Doccano 格式标注数据
- `evaluate`：金标准对撞计算 P/R/F1

### 2.2 技术栈总览

| 层次 | 技术选型 | 说明 |
|------|---------|------|
| 文本提取 | PyMuPDF + pdfminer.six + OpenAI Vision OCR | 三级容错策略 |
| 分词 | jieba | 中文分词 |
| 术语统计 | scikit-learn TF-IDF + 自研 PMI | 候选术语排序 |
| LLM 清洗 | OpenAI 兼容 API | 包容性提示词策略 |
| 实体识别 | 自研词典最长匹配 + 正则规则引擎 | 高召回率 |
| 关系抽取 | 自研触发词规则 + 四重约束过滤 | 高精确率 |
| 图谱存储 | JSON + CSV + rdflib TTL + Neo4j 5 | 多格式输出 |
| Web 后端 | FastAPI | RESTful API |
| Web 前端 | ECharts 5 + Particles.js + 原生 JS/CSS | 力导向图可视化 |
| 评估 | 自研 metrics.py | Precision/Recall/F1 |

### 2.3 断点续传设计

系统的所有中间产物均以文件形式落盘（`.csv`、`.jsonl`、`.json`），每个阶段独立运行、独立输出。这意味着：

- 修改了过滤规则后，只需重跑 `clean` 阶段，无需重跑耗时的 `extract`
- 修改了触发词后，只需重跑 `relation` 阶段
- 修改了 LLM 提示词后，只需重跑 `finalize` 阶段
- 任何阶段中断后可直接从该阶段恢复，拒绝重复计算

统一入口为 `scripts/run_pipeline.py`，通过 `--stage` 参数指定运行阶段。

---

## 第三章 本体设计

### 3.1 设计原则

本体设计遵循以下原则：

1. **领域聚焦**：仅覆盖变构飞行器及相关航空航天领域，不做泛化
2. **粒度适中**：实体类型不宜过细（导致标注困难）也不宜过粗（丧失语义区分度）
3. **关系可解释**：每种关系类型都有明确的语义定义和触发词支撑
4. **规模可控**：8 类实体 + 10 类关系，足够支撑 500+ 概念和 1000+ 关系的规模要求

### 3.2 实体类型定义（8 类）

| 类型 | 英文标识 | 定义 | 判定原则 | 代表性示例 |
|------|---------|------|---------|-----------|
| 飞行器 | `Aircraft` | 飞行平台、飞行器类别或具体机型 | 描述某种飞行平台或构型平台的术语 | 变体飞行器、跨域变构飞行器、无人机、空天飞行器 |
| 结构部件 | `Structure` | 飞行器的物理结构、部件、翼面、外形布局 | 明确属于飞行器部件或几何外形组成部分的术语 | 机翼、翼型、蒙皮、舵面、折叠构型、前缘缝翼 |
| 变形机制 | `Mechanism` | 变形方式、驱动方式、变构机理 | 强调"如何变""靠什么变"的术语 | 变后掠机制、折展变构、柔性翼变形、机构驱动 |
| 控制方法 | `ControlMethod` | 控制策略、优化算法、智能决策方法 | 具体控制方法或优化求解方法 | 模型预测控制(MPC)、滑模控制、自适应控制、DDPG |
| 性能指标 | `Performance` | 飞行性能、气动特性、稳定性与效率类指标 | 表示"飞得怎么样""控制得怎么样"的名词 | 升阻比、机动性能、鲁棒性、气动性能、动稳定性 |
| 飞行任务 | `Mission` | 飞行任务、任务阶段、任务目标 | 描述飞行器执行什么任务或处于什么阶段的术语 | 巡航、再入、机动、着陆、跨域飞行 |
| 物理参数 | `Parameter` | 飞行状态量、控制参数、气动参数等可量化变量 | 可作为模型输入或飞行状态量的术语 | 马赫数、迎角、展弦比、雷诺数、弦长 |
| 文献来源 | `Document` | 论文、文献、项目名称 | 明确的文献或项目来源名称 | SAW项目、SMS项目、文献[33] |

### 3.3 关系类型定义（10 类）

| 关系 | 英文标识 | 语义 | 触发词示例 | 类型约束 |
|------|---------|------|-----------|---------|
| 上下位 | `is_a` | A 是 B 的一种 | "是一种""属于""可视为" | 无限制 |
| 部分-整体 | `part_of` | A 是 B 的组成部分 | "是""组成部分""属于" | tail ∈ {Aircraft, Structure, Mechanism} |
| 包含部件 | `has_component` | A 包含/由 B 组成 | "包括""包含""由""组成" | head ∈ {Aircraft, Structure, Mechanism, ControlMethod} |
| 采用方法 | `uses_method` | A 采用方法 B | "采用""使用""基于""利用" | tail ∈ {ControlMethod, Mechanism} |
| 具有机制 | `has_mechanism` | A 具有变形/驱动机制 B | "具有""具备""通过" | tail ∈ {Mechanism} |
| 影响 | `affects` | A 影响 B | "影响""决定""改变""制约" | tail ∈ {Performance, Parameter, Mission} |
| 适用于 | `applies_to` | A 适用于 B | "适用于""用于""面向""针对" | head ∈ {ControlMethod, Mechanism} |
| 具有参数 | `has_parameter` | A 具有参数 B | "参数为""取值""设定为" | tail ∈ {Parameter} |
| 支持任务 | `supports_mission` | A 支持任务 B | "支持""服务于""满足" | tail ∈ {Mission} |
| 记载于 | `documented_in` | A 记载于文献 B | "见""记载于""发表于" | tail ∈ {Document} |

---

## 第四章 数据准备与预处理

### 4.1 语料来源

语料来源于课程提供的航空航天领域专著 PDF 文件集，涵盖：可重复使用新型航天飞行器结构设计、航天飞机空气动力学分析、飞行器气动布局设计、变构飞行器飞行控制等方向，共计数十篇核心文献。

### 4.2 PDF 文本提取（`preprocess/extract.py`，505 行）

这是整个系统中工程复杂度最高的模块。由于学术 PDF 的编码方式极其多样（存在大量国产排版工具如方正、WPS 生成的 CID 字体 PDF），本模块设计了一套**三级容错 + 乱码检测 + CID 强制解码**的提取策略。

#### 4.2.1 三级容错提取策略

每一页 PDF 按以下顺序尝试提取：

**策略 1：PyMuPDF rawdict 字符级提取**
- 使用 `page.get_text("rawdict")` 获取字符级原始数据
- 遍历每个 block → line → span → char，逐字符重建文本
- 同时使用 `page.get_text("text", sort=True)` 作为补充
- 对两种结果计算质量分数（`text_quality_score`），选择更优的版本

**策略 2：pdfminer.six 细粒度 CID 字体解析**
- 当 PyMuPDF 结果被判定为乱码时启动
- pdfminer.six 有独立的 CMap 重建逻辑，对无 ToUnicode 映射的字型更友好
- 会尝试从 font stream 中反推 GB2312/BIG5 映射

**策略 2.5：CID→GBK/GB2312 强制解码**（`_decode_cid_as_gbk`）
- 当 pdfminer 输出中仍包含 `(cid:XXXX)` 占位符时启动
- 将 CID 编号按大端序解释为 GBK 双字节编码
- 依次尝试 GBK → GB2312 → GB18030 → BIG5 解码
- 这一策略专门针对国产 PDF 制作工具的编码特性设计

**策略 3：OpenAI 兼容视觉模型 OCR**（兜底）
- 仅当前两级策略均输出乱码时才启动
- 将 PDF 页面渲染为 PNG 图片（zoom=1.5）
- 转为 base64 编码后发送给视觉模型
- 使用专门设计的 system_prompt 和 user_prompt 进行文字识别

#### 4.2.2 乱码检测算法（`is_garbled`）

该函数基于多维度统计指标判断文本是否为乱码：

- **汉字占比**（`han_ratio`）：中文文档中汉字比例低于 5% 且符号占比超过 25% 时判定为乱码
- **控制字符计数**：出现任何控制字符（`\x00-\x08` 等）直接判定为乱码
- **噪声 token 检测**：8 位以上连续字母数字混排串（如 `aXb3cD9e`）通常是乱码特征
- **有效字符比例**：总有效比例低于 45% 且汉字占比低于 12% 时判定为乱码

#### 4.2.3 并行处理

- 文本层扫描：默认 10 线程并行（`ThreadPoolExecutor`），逐页提取后合并
- OCR 识别：对需要 OCR 的页面同样支持多线程并发，减少等待时间

### 4.3 文本清洗与分句（`preprocess/clean.py`，43 行）

**`clean_text` 函数**：
- 统一换行符（`\r\n` → `\n`）
- 移除控制字符和不间断空格
- 过滤纯页码行（`\d{1,4}`）
- 过滤重复字符行（长度 ≥ 12 但唯一字符 ≤ 3）
- 合并多余空白

**`split_sentences` 函数**：
- 基于中文标点（`。！？；`）和英文标点（`!?;`）进行句子切分
- 兼容中文学术文献常见的分号断句习惯

**`tokenize_sentence` 函数**：
- 调用 jieba 进行中文分词

### 4.4 元数据生成

**文档元数据表** `data/metadata/documents.csv`：
- 为每篇文献分配唯一 `doc_id`（如 `DOC001`）
- 记录文件名、标题、主题分类（自动推断）

**句子级语料** `data/processed/sentences.jsonl`：
- 每行一条 JSON 记录，包含 `doc_id`、`title`、`file_name`、`sentence_id`、`text`
- 最终生成约 **104,910** 条句子记录

---

## 第五章 术语抽取与领域词表构建

### 5.1 候选术语生成（`terminology/term_extraction.py`，226 行）

#### 5.1.1 n-gram 候选生成

对全部语料的分词结果，生成 1-gram 到 4-gram 的所有候选短语（`generate_candidate_terms`）：

- 对每个 token 先进行有效性检查（`valid_token`）：长度 2~20 字符、匹配中文/英文 token 模式、不在停止词表中
- 生成候选后统计全语料出现频次
- 过滤低频候选（频次 < 2）

本阶段共生成约 **93,000** 条候选术语。

#### 5.1.2 停止词与噪声过滤

系统内置了多层停止词过滤体系（`is_noise_term` 函数）：

- **中文停止词表**（`TERM_STOPWORDS`）：49 个常见泛化词，如"研究""分析""设计""方法"
- **英文停止词表**（`ENGLISH_STOPWORDS`）：68 个英文通用词和 LaTeX 命令词
- **英文元数据词**（`ENGLISH_METADATA_TERMS`）：期刊名、机构名等
- **公式符号词**（`FORMULA_TERMS`）：数学符号名
- **英文白名单**（`ENGLISH_TERM_WHITELIST`）：`aircraft`、`morphing`、`uav`、`wing`、`mpc`
- **机构名过滤**：包含"学报""大学""出版社"等词的术语直接过滤
- **格式引用词过滤**：如"由式""如图""参考文献"等
- **驼峰拼接检测**：检测中英文混排的拼接错误（`CAMEL_JOIN_PATTERN`）

#### 5.1.3 TF-IDF 计算

使用 scikit-learn 的 `TfidfVectorizer`（`compute_tfidf_scores`）：
- `max_features=8000`
- 自定义 tokenizer 使用 jieba 分词
- 计算每个术语在全语料中的 TF-IDF 得分

#### 5.1.4 PMI 计算

自研的逐点互信息算法（`compute_pmi_scores`）：
- 对排名前 8,000 的候选术语计算 PMI
- 对每个候选术语，枚举所有可能的二分切割点（`left|right`）
- 计算 `PMI = log2((P(term) × N) / (P(left) × P(right)))`
- 取所有切割点中的最小 PMI 值（最弱连接点）
- PMI 值越高，说明术语内部搭配越紧密

#### 5.1.5 综合评分

每个候选术语的综合得分公式：
```
score = freq × 0.6 + tfidf × 0.3 + max(pmi, 0) × 0.1
```

按得分降序排列后，取前 **3,000** 条输出到 `terms_clean.csv`。

### 5.2 LLM 智能精筛（`terminology/llm_clean.py`，162 行）

#### 5.2.1 调用策略

- 从 `terms_clean.csv` 中取前 `top_n=5000` 条候选
- 每 `batch_size=80` 条为一批，调用 OpenAI 兼容 API
- 共分约 **38 个批次**
- 每个批次独立处理，支持断点续传

#### 5.2.2 提示词设计

采用**包容性提示词策略**（非严苛去噪策略），核心要求：

- **可删除**：纯动词、代词、拼凑长句
- **务必保留**：只要能代表一个具体事物、指标或概念，即便带有修饰语也保留
- **分类要求**：将每个保留的术语分入 8 类实体之一（含 Concept 兜底类）
- **归一化**：提取冗余术语的核心形态（如"飞行器结构及其设计" → "飞行器结构设计"）
- **输出格式**：严格的 JSON 数组

#### 5.2.3 输出结果

- `terms_clean_llm.csv`：LLM 审核通过的术语及修正后的类型/别名
- `alias_llm.csv`：LLM 生成的别名对照表

### 5.3 术语表定稿（`terminology/finalize_terms.py`，258 行）

这是术语工程的最终环节，将统计抽取结果与 LLM 清洗结果进行合并、过滤和定稿。

#### 5.3.1 过滤规则链

定稿脚本执行了 8 层过滤：

1. **触发词黑名单**（24 个）：如"包括""采用""影响"等关系抽取触发词不能作为实体
2. **噪声词硬编码黑名单**（50+ 个）：逻辑连词、地名、模糊量词等
3. **纯英文非白名单过滤**：非 `morphing/aircraft/wing/uav/mpc` 的纯英文词一律过滤
4. **长度过滤**：单字术语过滤
5. **纯数字过滤**
6. **格式词过滤**：`图X`、`表X`、`第X章` 等
7. **语用词正则过滤**（5 个正则模式）：如"如果""虽然"开头的词、过于泛化的物理量单字词
8. **LLM 未通过过滤**：如果 LLM 清洗阶段已运行但该词未出现在 LLM 输出中，说明被 LLM 判定为垃圾，直接丢弃

#### 5.3.2 类型二次推断

对通过过滤但仍为 `Concept` 类型的术语，使用更精细的规则推断类型（比初始的 `term_extraction.py` 中的规则更详细，包含 100+ 个关键词映射）。

#### 5.3.3 输出

- `data/terminology/terms_final.csv`：最终定稿术语表
- `data/terminology/alias_final.csv`：最终别名对照表

---

## 第六章 实体识别

### 6.1 实体识别模块（`candidate_extraction/ner.py`，242 行）

#### 6.1.1 词典匹配策略

核心函数 `predict_ner` 对每条句子执行实体识别：

1. **加载术语词典**：从 `terms_final.csv`（优先）或 `terms_clean.csv` 中加载所有 `keep=Y` 的术语，并按术语长度降序排列（**最长匹配优先**）
2. **滑动窗口扫描**：对句子中的每个字符位置，使用 `find_all_occurrences` 查找该术语的所有出现位置
3. **重叠检测**：通过 `overlaps` 函数检查新发现的实体 span 是否与已确认的实体重叠，重叠则跳过（保证最长匹配优先生效）
4. **输出**：每个命中记录包含 `entity`、`entity_type`、`start`（起始偏移）、`end`（结束偏移）、`source`（标记为 `dictionary`）

#### 6.1.2 正则规则补充

`infer_rule_entities` 函数提供三类正则补充识别：

| 正则模式 | 实体类型 | 说明 |
|---------|---------|------|
| `数字+单位（°/m/s/马赫/N/Pa）` | Parameter | 捕获带单位的数值参数 |
| `英文+控制` | ControlMethod | 如"PID控制""MPC控制" |
| `机翼/翼型/尾翼/机身/舵面/铰接机构` | Structure | 补充高频结构部件 |

#### 6.1.3 全量语料扫描

`predict_corpus` 函数对 `sentences.jsonl` 中的全部 104,910 条句子逐一执行 NER，输出：
- `entities_raw.jsonl`：原始实体标注结果
- `entities_clean.jsonl`：经过 `clean_entities` 清洗后的结果

### 6.2 实体清洗（`clean_entities`）

对 raw 实体进行以下过滤：
- 实体为触发词 → 过滤
- 实体长度 ≤ 1 → 过滤
- 纯数字 → 过滤
- 纯标点 → 过滤
- 同文档同句同实体去重
- 使用 `terms_final.csv` 回写实体类型（优先使用定稿词表中的类型）

最终输出约 **167,845** 条清洗后的实体标注记录。
